packages:
  - "apps/*"
  - "packages/*"

# pnpm 11 需要明确允许这些跨平台原生依赖执行安装脚本。
# 在 macOS 上会安装与当前 Node 架构一致的 darwin-arm64 或 darwin-x64 构建。
allowBuilds:
  better-sqlite3: true
  esbuild: true
  sharp: true
  # tesseract.js 的 postinstall 仅显示 OpenCollective 提示，不影响 OCR 运行；跨平台环境中明确跳过。
  tesseract.js: false
