services:
  - type: web
    name: lai-commerce-studio-test
    runtime: node
    plan: free
    region: singapore
    branch: main
    autoDeployTrigger: off
    buildCommand: corepack enable && corepack prepare pnpm@11.19.0 --activate && pnpm install --frozen-lockfile && pnpm --filter @lai/web build
    startCommand: pnpm --filter @lai/web exec next start -H 0.0.0.0 -p $PORT
    healthCheckPath: /api/v1/health
    envVars:
      - key: DATABASE_PATH
        value: /tmp/laicommerce.db
      - key: LAI_SEED_MODE
        value: blank
      - key: LAI_TEXT_PROVIDER
        value: openrouter
      - key: OPENROUTER_MODEL
        value: openrouter/free
      - key: OPENROUTER_SITE_URL
        value: https://lai-commerce-studio-test.onrender.com
      - key: OPENROUTER_APP_NAME
        value: LaiCommerce Studio
      - key: OPENROUTER_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5.6-sol
      - key: OPENAI_REASONING_EFFORT
        value: low
      - key: OPENAI_API_KEY
        sync: false
      - key: WORKBENCH_ACCESS_TOKEN
        sync: false
      - key: AGENT_TOKEN_PEPPER
        generateValue: true
      - key: WEBHOOK_SECRET
        generateValue: true
