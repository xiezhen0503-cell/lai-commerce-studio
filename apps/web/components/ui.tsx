import clsx from "clsx";

export function Badge({ children, tone = "gray" }: { children: React.ReactNode; tone?: "jade"|"amber"|"red"|"indigo"|"gray" }) { return <span className={clsx("badge", tone)}>{children}</span>; }
export function PageHead({ eyebrow, title, subtitle, action }: { eyebrow: string; title: string; subtitle: string; action?: React.ReactNode }) { return <div className="page-head"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1><p className="subtitle">{subtitle}</p></div>{action}</div>; }
export function EmptyState({ title, text, action }: { title: string; text: string; action?: React.ReactNode }) { return <div className="card card-pad" style={{textAlign:"center",padding:"44px 24px"}}><h2>{title}</h2><p className="subtitle" style={{maxWidth:480,margin:"0 auto 18px"}}>{text}</p>{action}</div>; }
export const statusTone = (status: string): "jade"|"amber"|"red"|"indigo"|"gray" => status === "approved" || status === "succeeded" || status === "confirmed" || status === "parsed" || status === "verified" || status === "user-confirmed" ? "jade" : status === "failed" || status === "rejected" || status === "blocked" || status === "quarantined" || status === "expired" || status === "revoked" ? "red" : status === "running" || status === "active" || status === "parsing" ? "indigo" : "amber";
export const statusLabel = (status: string) => ({ draft:"草稿", "needs-input":"待完善", active:"进行中", revoked:"已撤销", "needs-review":"待审核", completed:"已完成", approved:"已批准", rejected:"已驳回", stale:"可能过期", queued:"排队中", running:"生成中", succeeded:"已完成", failed:"解析失败", quarantined:"已隔离", cancelled:"已取消", pending:"待处理", confirmed:"已确认", uploaded:"已上传", parsing:"解析中", parsed:"已解析", missing:"缺失", conflicting:"冲突", expired:"来源已失效", verified:"有来源", inferred:"待确认", "user-confirmed":"用户确认" } as Record<string,string>)[status] || status;
