import { getCommerceService } from "@lai/shared";
import { Badge, PageHead } from "@/components/ui";
export const dynamic="force-dynamic";
export default function AgentLogsPage(){const events=getCommerceService().repo.list<any>("audit_events").filter(event=>event.actorType==="agent"||event.action.startsWith("agent."));return <><PageHead eyebrow="Audit Trail" title="智能体调用日志" subtitle="只记录脱敏摘要、权限与 Trace ID；完整 Token 和模型密钥不会进入日志。"/><div className="card table-wrap"><table><thead><tr><th>时间</th><th>智能体</th><th>动作</th><th>输入 / 输出摘要</th><th>Trace ID</th></tr></thead><tbody>{events.map(event=><tr key={event.id}><td>{new Date(event.createdAt).toLocaleString("zh-CN")}</td><td>{event.actorId}</td><td><Badge tone="indigo">{event.action}</Badge></td><td className="mono small">{event.summary}</td><td className="mono">{event.traceId}</td></tr>)}{events.length===0&&<tr><td colSpan={5} className="muted" style={{textAlign:"center",padding:36}}>尚无外部智能体调用；创建连接后可用演示 Token 验证。</td></tr>}</tbody></table></div></>}
