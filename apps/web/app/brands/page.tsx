import { getCommerceService } from "@lai/shared";
import { Badge, PageHead } from "@/components/ui";
export const dynamic="force-dynamic";
export default function BrandsPage(){const brands=getCommerceService().repo.listBrands();return <><PageHead eyebrow="Brand Assets" title="品牌资产中心" subtitle="只有人工确认后的语言与视觉规则，才会进入正式生成链路。"/><div className="grid-3">{brands.map(brand=><div className="card card-pad" key={brand.id}><div className="row between"><div className="brand-glyph">{brand.name.slice(0,1)}</div><Badge tone="jade">已确认</Badge></div><h2 style={{margin:"18px 0 6px"}}>{brand.name}</h2><p className="subtitle">{brand.positioning}</p><div className="row" style={{marginTop:18}}>{brand.colors.slice(0,4).map(color=><span key={color} title={color} style={{width:24,height:24,borderRadius:8,background:color,border:"1px solid rgba(0,0,0,.08)"}}/>)}</div><div className="list-meta" style={{marginTop:14}}>品牌语气：{brand.tone.join(" · ")}</div></div>)}</div></>}
