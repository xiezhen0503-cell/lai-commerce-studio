import { getCommerceService } from "@lai/shared";
import { Badge, PageHead } from "@/components/ui";
export const dynamic="force-dynamic";
export default function ProductsPage(){const products=getCommerceService().repo.listProducts();return <><PageHead eyebrow="Products" title="商品资料中心" subtitle="规格、价格、库存、卖点与证据分开管理；未确认字段不会混入可发布内容。"/><div className="card table-wrap"><table><thead><tr><th>商品</th><th>SKU</th><th>规格</th><th>库存</th><th>证据</th><th>状态</th></tr></thead><tbody>{products.map(product=><tr key={product.id}><td><strong>{product.name}</strong><div className="list-meta">{product.category}</div></td><td className="mono">{product.sku}</td><td>{product.specification}</td><td>{product.inventory??"缺失"}</td><td>{product.evidence.length} 项</td><td><Badge tone={product.status==="confirmed"?"jade":"amber"}>{product.status==="confirmed"?"已确认":"待资料确认"}</Badge></td></tr>)}</tbody></table></div></>}
