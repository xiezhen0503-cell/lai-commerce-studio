# 小赖电商工作台 Codex Plugin

插件包含 17 个电商内容生产技能，并连接本仓库的本地 MCP 服务。

1. 在仓库根目录运行 `pnpm dev:mcp`。
2. 将 `LAI_COMMERCE_TOKEN` 设置为工作台签发的 Agent Token；本地演示可使用种子数据中的演示令牌。
3. 在 Codex 中安装 `plugins/lai-commerce-studio`。

默认只生成草稿和任务记录。公开发布、付费调用、删除或权限变更必须由用户明确确认。
